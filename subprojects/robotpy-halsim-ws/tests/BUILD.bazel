load("@mostrobotpy_tests_pip_deps//:requirements.bzl", "requirement")
load("@rules_semiwrap//:defs.bzl", "robotpy_py_test")

robotpy_py_test(
    "halsim_ws_tests",
    extra_sources = ["conftest.py"],
    tests = glob(
        ["**/*.py"],
        exclude = [
            "conftest.py",
            "run_tests.py",
        ],
    ),
    deps = [
        requirement("pytest"),
        "//subprojects/robotpy-halsim-ws:import",
    ],
)
