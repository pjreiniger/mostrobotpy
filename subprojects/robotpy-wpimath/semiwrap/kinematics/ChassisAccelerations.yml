classes:
  wpi::math::ChassisAccelerations:
    force_no_default_constructor: true
    attributes:
      ax:
      ay:
      alpha:
    methods:
      ToRobotRelative:
      ToFieldRelative:
      operator+:
      operator-:
        overloads:
          const ChassisAccelerations& [const]:
          '[const]':
      operator*:
      operator/:
      operator==:
